{
  "name": "gpx-ascii-map",
  "version": "1.0.0",
  "description": "Render GPX routes as ASCII art maps with shareable links",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.2.0",
    "uuid": "^11.1.1"
  }
}
